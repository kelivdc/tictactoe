<div class="mx-auto w-[400px]">
  <div class="flex p-4 space-x-8">
    <div>
      <%= render @game %>
    </div>
    <div>
      <%= render "board" %>
    </div>
  </div>
  <div class="flex justify-between">
    <%= link_to "New Player", new_game_url, class: "rounded-lg py-1 px-2 bg-blue-400 hover:bg-blue-500 inline-block font-medium"%>
    <%= link_to "Rematch", game_path(@game), class: "rounded-lg py-1 px-2 bg-blue-400 hover:bg-blue-500 inline-block font-medium"%>
  </div>
</div>
